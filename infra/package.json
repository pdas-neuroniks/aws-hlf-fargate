{
  "name": "infra",
  "version": "0.1.0",
  "bin": {
    "infra": "bin/infra.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "bootstrap": "cdk bootstrap --profile neuroniks-dev",
    "synth": "cdk synth",
    "deploy": "cdk synth && cdk deploy --require-approval never --all --profile neuroniks-dev --outputs-file ../cdk-outputs.json",
    "destroy": "cdk destroy --require-approval never --all --profile neuroniks-dev",
    "push": "git add . && git commit -m 'backend push' && git push"
  },
  "devDependencies": {
    "@types/git-branch": "^2.0.5",
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1029.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@aws-cdk/aws-managedblockchain": "^1.204.0",
    "@cdklabs/cdk-hyperledger-fabric-network": "^0.8.990",
    "aws-cdk-lib": "2.213.0",
    "constructs": "^10.0.0",
    "git-branch": "^2.0.1"
  }
}
